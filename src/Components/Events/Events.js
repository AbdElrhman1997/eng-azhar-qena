import React from "react";
import "./Events.scss";

const Events = () => {
  return (
    <section className="events container">
      <p>لا توجد فعاليات</p>
    </section>
  );
};

export default Events;
